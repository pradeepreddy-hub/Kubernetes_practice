apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: demo-eks-cluster
  region: ap-south-1

managedNodeGroups:
  - name: managed-ng
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 4
